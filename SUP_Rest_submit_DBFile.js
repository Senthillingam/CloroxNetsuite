function save_db(dataIn)
{
	nlapiLogExecution('Debug','save_db','dataIn == '+dataIn);
	
	if(dataIn!=null)
	{
		var blobValue = dataIn.blobpara;
		nlapiLogExecution('Debug','save_db','blobValue == '+blobValue);
		
	}
}